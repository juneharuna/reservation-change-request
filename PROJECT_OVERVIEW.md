# [현대 인증 중고차] 방문평가 일정변경 요청 시스템 상세 가이드

## 🌟 1. 프로젝트 개요 및 비전

본 프로젝트는 **현대 인증 중고차(Hyundai Certified Used Car)** 서비스의 효율적인 운영을 위해 설계된 핵심 운영 지원 도구입니다.

차량 매입 전 필수 단계인 '방문평가' 과정에서 발생하는 일정 변경 이슈를 표준화된 인터페이스로 해결합니다. 영업 현장의 **카마스터(요청자)**와 내부 **운영 파트너(검토자)** 사이의 불필요한 커뮤니케이션 비용을 줄이고, 데이터 기반의 정확한 처리를 목적으로 합니다.

---

## � 2. 상세 기능 및 사용자 여정 (User Journey)

### A. 카마스터 (방문평가 대상 차량 담당자)
1.  **차량 조회 (Search Step)**
    *   차량 번호(예: 123가4567)를 입력하여 현재 진행 중인 일정이 있는지 조회합니다.
    *   **로직**: 입력 시 공백이 자동으로 제거되며, 이미 진행 중인 요청이 있다면 상태 페이지로, 없다면 신청 폼으로 자동 이동합니다.
2.  **변경 신청 (Form Step)**
    *   **업체 선택**: BO 확인 후 담당 방문평가업체(카뷰, CTS 등)를 지정합니다.
    *   **희망 일정**: 직관적인 캘린더와 버튼(오전/오후/직접선택)을 사용하여 새로운 희망 일시를 지정합니다.
    *   **사유 입력**: 10자 이상의 구체적인 사유 입력을 강제하여 검토 효율성을 높였습니다.
3.  **실시간 상태 추적 (Status Step)**
    *   신청한 내용이 어떤 단계(`접수 대기` → `진행 중` → `성공/불가`)에 있는지 직관적인 스테퍼(Stepper)로 확인합니다.
    *   **취소 기능**: 업체가 검토를 완료하기 전에는 언제든지 요청을 자발적으로 취소할 수 있습니다.

### B. 파트너/관리자 (요청 검토 및 확정 담당자)
1.  **운영 대시보드 (Dashboard)**
    *   전체 요청 현황을 실시간 카운트로 확인합니다 (예: 현재 일정조정 진행 중인 건수 등).
    *   필터링 기능을 통해 신규 접수된 건들만 모아서 빠르게 처리할 수 있습니다.
2.  **요청 처리 (Adjustment)**
    *   카마스터의 사유를 확인한 뒤, 내부 시스템과 대조하여 일정을 확정하거나 거절합니다.
    *   **성공 처리**: 최종 확정된 날짜와 시간을 입력하면 카마스터 화면에 '조정 완료'로 반영됩니다.
    *   **거절 처리**: 거절 사유(업체 사정, 정보 불일치 등)를 명시하여 카마스터에게 즉시 피드백을 전달합니다.

---

## 🧠 3. 핵심 비즈니스 로직 (Core Logic)

시스템의 안정성과 데이터 정확성을 위해 설계된 세부 로직들입니다.

### 📱 데이터 유효성 검사 (Validation)
*   **전화번호 자동 포맷팅**: 사용자가 숫자만 입력해도 `010-0000-0000` 형태로 실시간 하이픈(-)이 삽입되어 오타를 방지합니다.
*   **제약 조건**: 모든 필수 항목(업체 선택, 10자 이상의 사유, 8자리 이상의 연락처)이 채워져야만 '변경 요청' 버튼이 활성화됩니다.
*   **날짜 제한**: 과거의 날짜로는 일정 변경을 신청할 수 없도록 현재 날짜 기준의 최소값(Min Date)이 설정되어 있습니다.

### 🔄 상태 관리 머신 (Status Flow)
요청은 아래의 정해진 흐름에 따라서만 상태가 변화합니다:
*   **PENDING (접수 대기)**: 카마스터가 막 신청한 상태. 수정/취소가 자유롭습니다.
*   **RECEIVED (진행 중)**: 업체가 내용을 확인하고 고객과 일정 조율을 시작한 상태.
*   **SUCCESS (조정 완료)**: 최종 조율이 끝나 방문 일정이 확정된 완성 상태.
*   **FAILED (조정 불가)**: 조율이 무산된 상태. 상세 사유가 함께 표시됩니다.
*   **TERMINATED (확인 불가)**: 존재하지 않는 차량번호 등 데이터 오류로 종결된 특수 상태.

---

## 🛠 4. 기술 스택 및 아키텍처 상세

### 🎨 Frontend (사용자 인터페이스)
*   **React 19 & Vite**: 최신 리액트 환경으로 구축되어 부드러운 애니메이션과 즉각적인 반응을 제공합니다.
*   **Mantine UI Framework**: 현대적인 디자인 시스템을 기반으로 레이아웃과 컴포넌트를 구성하여 프리미엄 급 UI를 완성했습니다.
*   **Lucide React**: 20여 종의 시맨틱 아이콘을 사용하여 텍스트 없이도 정보를 쉽게 파악할 수 있게 했습니다.
*   **Zustand (State Management)**: 가벼우면서도 강력한 상태 관리 라이브러리를 사용하여 사용자의 입력 데이터가 페이지 전환 시에도 유지되도록 설계했습니다.

### 🔌 Backend & Deployment (서버 및 운영)
*   **Express 정적 서버**: 로컬에서 빌드된 정적 파일(`dist`)을 효율적으로 서빙하며, 새로고침 시 경로를 유지해 주는 SPA 라우팅 로직이 포함되어 있습니다.
*   **GCP VM (Google Cloud)**: 높은 가용성을 위해 구글 클라우드 환경에 배포되었습니다. 
*   **PM2 (Process Manager)**: 서버 프로세스가 24시간 중단 없이 실행되도록 감시하며, 예기치 못한 에러로 종료될 경우 즉시 자동 재시작합니다.
*   **Firewall Optimization**: 8080 포트를 독립적으로 사용하여 기존 서비스(`heydealer`)와 물리적으로 분리된 안전한 운영 환경을 제공합니다.

---

## 🚀 5. 유지보수 및 확장 (Maintenance)

본 프로젝트는 추후 실제 데이터베이스(MySQL, MongoDB 등) 연동을 고려하여 코드 수준에서 데이터 모델이 정형화되어 있습니다. 현재는 **초경량 배포 방식(`reservation.zip`, ~1.1MB)**을 사용하여 아주 적은 자원으로도 안정적인 운영이 가능하도록 최적화되어 있습니다.
